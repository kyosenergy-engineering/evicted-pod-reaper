kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  # Port mapping for Prometheus metrics if needed
  extraPortMappings:
  - containerPort: 30080
    hostPort: 30080
    protocol: TCP